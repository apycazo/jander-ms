spring:
    application.name: quotes-svc
    profiles.active: defaults
    cloud.config:
        uri: http://localhost:8761/cloud
        server.prefix: cloud
        # username: root
        # password: s3cr3t
